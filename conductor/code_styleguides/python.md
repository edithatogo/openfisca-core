# Python Style

- Follow the repository's `STYLEGUIDE.md`, `setup.cfg`, and `pyproject.toml`.
- Use Black-compatible formatting, configured import ordering, and existing
  docstring conventions.
- Avoid mutable default arguments, bare exceptions, and mutable global state.
- Add type annotations where they improve a public contract without conflicting
  with established project practice.
